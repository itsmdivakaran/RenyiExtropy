% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renyi_extropy.R
\name{renyi_extropy}
\alias{renyi_extropy}
\title{Calculate the Rényi Extropy}
\usage{
renyi_extropy(p, q)
}
\arguments{
\item{p}{Numeric vector. Probability vector. Elements must be in [0, 1] and sum to 1.}

\item{q}{Numeric scalar. Rényi parameter. Real number, not equal to 1.}
}
\value{
Numeric value. The calculated Rényi extropy.
}
\description{
Computes the Rényi extropy for a given probability vector and Rényi parameter.
}
\details{
The Rényi extropy of order \eqn{q} is defined as:
\deqn{J_q(P) = \frac{-(n-1)\log(n-1) + (n-1)\log\left( \sum_{i=1}^n (1 - p_i)^q \right)}{1 - q}}
where \eqn{n} is the number of elements in \eqn{P}.

For \eqn{q \to 1}, the function returns the Shannon extropy:
\deqn{J(P) = -\sum_{i=1}^n (1 - p_i) \log(1 - p_i)}.

For \eqn{n = 2}, Rényi extropy equals Rényi entropy.

Throws an error if \code{p} is not a valid probability vector or if \code{q} is not a real number.
}
\examples{
p <- c(0.2, 0.5, 0.3)
renyi_extropy(p, 2)
renyi_extropy(p, 1.1)

}
\seealso{
\code{\link{shannon_entropy}}, \code{\link{renyi_entropy}}, \code{\link{extropy}}
}
